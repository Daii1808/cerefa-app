{
  "name": "cerefa-app-root",
  "version": "1.0.0",
  "description": "Root package.json to proxy build commands to frontend directory for Vercel",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  }
}
